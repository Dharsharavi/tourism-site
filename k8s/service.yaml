apiVersion: v1
kind: Service

metadata:
  name: tourism-site

spec:
  type: NodePort

  selector:
    app: tourism-site

  ports:
    - name: app
      port: 8081
      targetPort: 8081
      nodePort: 30081

    - name: actuator
      port: 9091
      targetPort: 9091
      nodePort: 30091
